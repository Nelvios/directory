export class Seat {
    id: number;
    name: string;
    x: number;
    y: number;
    isVacant: boolean;
    isCubicle: boolean;

    // position() {
    //     return `left: ${this.x}%; top: ${this.y}%;`;
    // }
}